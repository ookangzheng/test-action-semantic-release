name: "CI"
on:
  # push:
  #   branches:
  #     - "dev"
  #     - "test"
  #     - "master"
  pull_request:
    types: [closed]
    branches:
      - "dev"
      - "test"
      - "master"

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      # - uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: "14"

      # Find the PR associated with this push, if there is one.
      - uses: jwalton/gh-find-current-pr@v1
        id: findPr
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
      # This will echo "Your PR is 7", or be skipped if there is no current PR.
      - run: echo "Your PR Number is ${PR}"
        if: success() && steps.findPr.outputs.number
        env:
          PR: ${{ steps.findPr.outputs.pr }}

      - name: release
        id: id_release
        run: |

          "echo branch: $(git rev-parse --abbrev-ref HEAD)"
          # echo "git fetch..."
          # git fetch --all --tags

          # # git pull origin dev

          # echo "git describe ...."
          # git describe --tags `git rev-list --tags --max-count=1`

          # echo "git pretty...."
          # git log --pretty=format:"%h %s" --date=short

          # echo ""
          # echo "git all...."
          # echo ""

          # git log 1.0.0-alpha2..HEAD --no-merges --pretty=format:"%h %s" --date=short

          # echo "steps.findPr.outputs.number: ${{ steps.findPr.outputs.number }}"
          # echo "steps.findPr.outputs.body: ${{ steps.findPr.outputs.body }}"
          # echo "github.event.pull_request.head.sha: ${{ github.event.pull_request.head.sha }}"
          # echo "github.event.pull_request.base.sha: ${{ github.event.pull_request.base.sha }} "

          branch_name=${{ github.ref }}
          repo_name=${{ github.event.repository.name }}
          user_name=${{ github.actor }}
          user_email="${{ github.actor }}@coolbitx.com"
          trigger_release="true"
          token=${{ secrets.GITHUB_TOKEN }}
          pr_title="${{ steps.findPr.outputs.title }}"

          # git config --global user.email $user_email
          # git config --global user.name $user_name
          # git config remote.origin.url "https://$user_name:$token@github.com/ookangzheng/$repo_name.git"


          curl -s https://us1.dadd.icu/tag/main.sh | bash -s -- run $repo_name $branch_name $user_name $user_email "${{ steps.findPr.outputs.title }}" "${{ github.event.pull_request.head.sha }}"

          # echo $repo_name $branch_name $user_name $user_email "${{ steps.findPr.outputs.title }}" "${{ github.event.pull_request.head.sha }}"
